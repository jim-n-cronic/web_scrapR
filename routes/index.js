const express = require('express');
const router = express.Router();
const db = require('../models');

// get route to root, populating INDEX.HANDLEBARS woth articles

// EXPORT ROUTER
module.exports = router;