const express = require('express');
const router = express.Router();
const db = require('../models');

// route to retrieve all notes for particular article

// route to return a single note to then view it

// post route to create new note in database

//  post route to delete a note

// EXPORT ROUTER
module.exports = router;