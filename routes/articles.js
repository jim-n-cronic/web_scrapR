const express = require('express');
const router = express.Router();
const db = require('../models');

// rotue to update SAVED boolean to TRUE

// route to render SAVEDARTICLES.HANDLEBARS and populate with saved articles

//delete route to remove from savedArticles

// EXPORT ROUTER
module.exports = router;